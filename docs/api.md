# API

The FastAPI service will expose the Agent and RAG capabilities.

## Planned Endpoints

- `GET /api/health`
- `POST /api/chat`
- `POST /api/projects/index`
- `POST /api/projects/search`

Detailed request and response schemas are implemented in Phase 3.
